generator:
  class: sfDoctrineGenerator
  param:
    model_class:           News
    theme:                 ecollegeplus
    non_verbose_templates: true
    with_show:             false
    singular:              ~
    plural:                ~
    route_prefix:          news
    with_doctrine_route:   true
    actions_base_class:    sfActions

    config:
      actions: ~
      fields:
        user:                 { label: Announcer }
      filter:
        class: false
      list:
        title:                  Institution News
        display:                [heading, blurb, is_published]
        sort:                   [heading, asc]
        batch_actions:          []
        object_actions:        
          _edit: ~
          _delete: ~
        max_per_page:			      10
        actions:
          _new: ~  
      form:
        display:                [heading, blurb, description, is_published, lock_until, lock_after]
      edit:
        actions:          
          cancel:               []
          done:                 []
          _save:                []
      new:
        actions:
          cancel:               []
          _save:                []