generator:
  class: sfDoctrineGenerator
  param:
    model_class:           Profile
    theme:                 popup
    non_verbose_templates: true
    with_show:             false
    singular:              ~
    plural:                ~
    route_prefix:          profile_details
    with_doctrine_route:   true
    actions_base_class:    sfActions

    config:
      fields:
      actions:             ~
      form:
        class:             ProfileDetailsForm
        display:           [ title, first_name, middle_name, last_name, gender, birth_date ]
      filter:
        class:             false
      edit:
        actions:
          cancel:          []
          done:            []
          _save:           []
      new:
        actions:
          cancel:          []
          save:            []